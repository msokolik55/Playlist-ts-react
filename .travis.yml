language: node_js
node_js:
  - lts/*
install:
  - cd frontend
  - npm ci
script:
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: frontend/dist
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main
